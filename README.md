In JS document.createElemnt create a new HTML element using JS.

JSON.parse this converts data to normal form which was in JSON form.

checkBox.addEventListener("change", () =>{
        todo.completed = checkBox.checked; Here, when the checkbox is clicked it will run the code 
        and update the status.

document.createElement('input'); //If we do any changes or add new content it will also reflect on HTML page.